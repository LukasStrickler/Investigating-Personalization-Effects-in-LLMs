# Linear Probing Pipeline — Dependencies
# ----------------------------------------
# Core ML / numerical
torch>=2.0
transformers @ git+https://github.com/huggingface/transformers.git
sentencepiece
protobuf
accelerate>=0.27
safetensors
huggingface_hub>=0.25

# Probing classifiers
scikit-learn>=1.3
numpy>=1.24

# Visualization
matplotlib>=3.7

# Progress bars
tqdm>=4.65

# Optional: for Flash Attention 2 support (requires CUDA)
# flash-attn>=2.5; platform_system != "Darwin"

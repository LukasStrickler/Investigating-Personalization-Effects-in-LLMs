# Config Directory

This directory contains configuration files for inference runs.

Example config will be implemented in Task 8:
- `inference.example.yaml` - Example configuration with provider presets
